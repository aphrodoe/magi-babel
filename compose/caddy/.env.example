# Real values live in secrets/caddy.env, which is gitignored and must exist on
# MAGI as well as cipher — git will not carry it there. Copy it with scp.
#
# deSEC API token: desec.io -> Token management -> create.
# Scoped to the lab.akhildhyani.me zone only; it can edit nothing else.
DESEC_TOKEN=replace-me
