This directory is gitignored. Nothing in here is ever committed.

Real secrets live here; every stack keeps a committed `compose/<stack>/.env.example`
with the *keys* and dummy values, so a rebuild knows what it needs to fill in.
